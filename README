Uses Spring AOP + AspectJ to log transaction status of any executed method within classes and subclasses of base package specified in Aspect. Useful for locating cases of unnecessary transaction management of methods that do not contain transactional calls.